<% if (post.link){ %>
  <h1 class="entry-title">
    <a class="<%= class_name %>" href="<%= post.permalink %>" target="_blank" itemprop="url"><%= post.title %></a>
  </h1>
<% } else if (post.title){ %>
  <% if (index){ %>
    <h1 class="entry-title">
      <a class="<%= class_name %>" href="<%= post.permalink %>"><%= post.title %></a>
    </h1>
  <% } else { %>
    <h1 class="entry-title <%= class_name %>">
      <%= post.title %>
    </h1>
  <% } %>
<% } %>